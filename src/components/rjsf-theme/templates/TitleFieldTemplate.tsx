import { TitleFieldProps } from '@rjsf/utils';

const TitleFieldTemplate: React.FC<TitleFieldProps> = () => {
  // Don't render any title - titles handled by FieldTemplate
  return null;
};

export default TitleFieldTemplate;
