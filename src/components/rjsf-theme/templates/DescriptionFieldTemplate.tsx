import { DescriptionFieldProps } from '@rjsf/utils';

const DescriptionFieldTemplate: React.FC<DescriptionFieldProps> = () => {
  // Don't render description separately - handled by FieldTemplate
  return null;
};

export default DescriptionFieldTemplate;
